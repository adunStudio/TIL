# 가운데 글자 가져오기



```cpp
#include <string>
#include <vector>

using namespace std;

string solution(string s)
{
    int length = s.length();

    if (length & 1 == 1) // 홀수
    {
        return s.substr(length / 2, 1);
    }
    else 
    {
        return s.substr(length / 2 - 1, 2);
    }
}
```

